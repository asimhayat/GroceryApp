import 'package:flutter/material.dart';

class EggDairyScreen extends StatelessWidget {
  const EggDairyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
